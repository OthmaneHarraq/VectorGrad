# VectorGrad
Customized implementation from scratch of a Residual MLP with custom autograd engine, AdamW optimizer, and cross-entropy loss in NumPy. Trained and evaluated on the ASL Sign Language MNIST dataset with 24-class classification, resulting in a 99.95% test accuracy.
